import { Container, Heading, HStack } from "@chakra-ui/react";
import Nav from "../Nav/Nav";

const Books = () => {
  return (
      <HStack>
        <Nav activeLink="Books"></Nav>
      </HStack>
  );
};

export default Books;
